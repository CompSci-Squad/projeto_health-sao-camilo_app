import { Text } from "react-native";

type DocumentPickerProps = {
  content: any;
  setContent: React.Dispatch<any>;
};

const DocumentPicker: React.FC<DocumentPickerProps> = ({
  content,
  setContent,
}) => {
  return <Text>Sla</Text>;
};

export default DocumentPicker;
