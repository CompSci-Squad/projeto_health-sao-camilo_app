import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    paddingLeft: 16,
    fontSize: 16,
    borderColor: "#619776",
    borderWidth: 2,
    borderRadius: 50,
    paddingVertical: 4,
    marginTop: 12,
  },
});

export default styles;
